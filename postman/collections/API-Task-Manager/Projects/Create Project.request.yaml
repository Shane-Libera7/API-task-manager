$kind: http-request
name: Create Project
url: "{{baseUrl}}/projects"
method: POST
headers:
  Authorization: Bearer {{accessToken}}
body:
  type: json
  content: |-
    {
        "name":"become healthy" 
    }
scripts:
  - type: afterResponse
    code: |-
      const res = pm.response.json();
      if (res.id) pm.collectionVariables.set("projectId", res.id);
    language: text/javascript
order: 1772544784284
