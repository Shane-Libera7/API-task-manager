$kind: http-request
name: Change project name
url: "{{baseUrl}}/projects/{{projectId}}"
method: PATCH
headers:
  Authorization: Bearer {{accessToken}}
body:
  type: json
  content: |-
    {
        "name": "save New York"
    }
order: 4497008675960731
