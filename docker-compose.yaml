version: '3.8'

services:
  db:
    image: postgres:15
    ports:
      - "5432:5432"
    environment: 
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password
      POSTGRES_DB: task_manager
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
